import styled from "styled-components";

export const Wrapper = styled.div`
  display: flex;
  gap: 10px;

  p {
    margin: 0;
    font-size: 20px;
    font-weight: 500;
  }

  button {
    background-color: inherit;
    border: none;
    cursor: pointer;
    text-align: center;
    display: flex;
    /* padding: 10px; */
  }
`;